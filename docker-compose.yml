services:
  app:
    build: .
    ports:
      - "30333:30333"
    volumes:
      - ${DOWNLOAD_FOLDER}:/downloads
    environment:
      - DEFAULT_DOWNLOAD_PATH=/downloads
    restart: unless-stopped
